# Auto-generated by scaffold.py -- edit as needed for this analysis.
# All scripts run through: pixi run py script.py

[workspace]
name = "{name}"
version = "0.1.0"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]

[dependencies]
python = ">=3.11"
pandoc = ">=3.0"
pandoc-crossref = ">=0.3"

# --- Core stack (always needed) ---
[pypi-dependencies]
uproot = ">=5.0"
awkward = ">=2.0"
hist = ">=2.7"
boost-histogram = ">=1.4"
numpy = ">=1.24"
scipy = ">=1.11"
matplotlib = ">=3.8"
mplhep = ">=1.1.0"
vector = ">=1.1"
particle = ">=0.23"
rich = ">=13.0"
iminuit = ">=2.25"
# --- Uncomment groups below as needed ---
# # Statistical models and fitting
# pyhf = ">=0.7"
# cabinetry = ">=0.6"
# # Unbinned likelihood fits
# zfit = ">=0.20"
# # MVA / classification
# xgboost = ">=2.0"
# scikit-learn = ">=1.3"
# # Columnar event processing at scale
# coffea = ">=2024.0"

[tasks]
py = "python"
build-pdf = "cd phase5_draft/outputs && pandoc ANALYSIS_NOTE.md -o ANALYSIS_NOTE.tex --standalone --include-in-header=../../conventions/preamble.tex --number-sections --toc --filter pandoc-crossref --citeproc && python ../../scripts/postprocess_tex.py ANALYSIS_NOTE.tex ANALYSIS_NOTE_pp.tex && tectonic ANALYSIS_NOTE_pp.tex"
lint-plots = "python scripts/lint_plots.py ."
# --- Phase tasks: add scripts as you write them ---
p0 = "echo 'phase0: no scripts yet'"
p1 = "echo 'phase1: no scripts yet'"
p2 = "echo 'phase2: no scripts yet'"
p3 = "echo 'phase3: no scripts yet'"
p4 = "echo 'phase4: no scripts yet'"
p5 = "echo 'phase5: no scripts yet'"
p6 = "echo 'phase6: no scripts yet'"
p7 = "echo 'phase7: no scripts yet'"
p0-plots = "echo 'phase0 plots: no scripts yet'"
p1-plots = "echo 'phase1 plots: no scripts yet'"
p2-plots = "echo 'phase2 plots: no scripts yet'"
p3-plots = "echo 'phase3 plots: no scripts yet'"
p4-plots = "echo 'phase4 plots: no scripts yet'"
p5-plots = "echo 'phase5 plots: no scripts yet'"
p6-plots = "echo 'phase6 plots: no scripts yet'"
p7-plots = "echo 'phase7 plots: no scripts yet'"
